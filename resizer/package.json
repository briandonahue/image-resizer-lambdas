{
  "name": "resizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel ./lib/index.js --out-file index.js",
    "publish": "npm run build && node-lambda deploy",
    "start": "npm run build && node-lambda run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0"
  },
  "dependencies": {
    "aws-sdk": "^2.2.47",
    "gm": "^1.21.1"
  }
}
